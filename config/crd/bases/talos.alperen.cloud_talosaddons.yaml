---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: talosaddons.talos.alperen.cloud
spec:
  group: talos.alperen.cloud
  names:
    kind: TalosAddon
    listKind: TalosAddonList
    plural: talosaddons
    shortNames:
    - ta
    singular: talosaddon
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.helmRelease.chartName
      name: Chart
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TalosAddon is the Schema for the talosaddons API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TalosAddonSpec defines the desired state of TalosAddon.
            properties:
              clusterRef:
                description: ClusterRef references the TalosCluster this addon should
                  be installed on
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              helmRelease:
                description: HelmRelease defines the Helm chart to be installed
                properties:
                  chartName:
                    description: ChartName is the name of the Helm chart
                    type: string
                  releaseName:
                    description: ReleaseName is the name of the Helm release
                    type: string
                  repoURL:
                    description: RepoURL is the URL of the Helm repository
                    type: string
                  targetNamespace:
                    default: default
                    description: TargetNamespace is the namespace where the Helm chart
                      will be installed
                    type: string
                  values:
                    additionalProperties:
                      type: string
                    description: Values is a map of values to pass to the Helm chart
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  valuesFrom:
                    description: ValuesFrom references a ConfigMap or Secret containing
                      values
                    items:
                      description: ValueReference contains a reference to a resource
                        containing values.
                      properties:
                        key:
                          description: Key in the ConfigMap or Secret
                          type: string
                        kind:
                          description: Kind of the values reference (ConfigMap or
                            Secret)
                          enum:
                          - ConfigMap
                          - Secret
                          type: string
                        name:
                          description: Name of the ConfigMap or Secret
                          type: string
                      required:
                      - kind
                      - name
                      type: object
                    type: array
                  version:
                    description: Version is the version of the Helm chart
                    type: string
                required:
                - chartName
                - repoURL
                type: object
            required:
            - clusterRef
            - helmRelease
            type: object
          status:
            description: TalosAddonStatus defines the observed state of TalosAddon.
            properties:
              conditions:
                description: Conditions is a list of conditions for the addon
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastAppliedRevision:
                description: LastAppliedRevision is the revision of the last successfully
                  applied Helm chart
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this addon
                format: int64
                type: integer
              state:
                description: State represents the current state of the addon
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
